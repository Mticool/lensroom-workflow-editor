{
  "buildCommand": "npm run build",
  "ignoreCommand": "test ! -d ./examples/ && test ! -d ./.local/ || (git diff --quiet HEAD^ HEAD ./examples/ 2>/dev/null || true) && (git diff --quiet HEAD^ HEAD ./.local/ 2>/dev/null || true)",
  "functions": {
    "api/**": {
      "excludeFiles": "{examples,.local}/**/*.json"
    },
    "api/workflow.js": {
      "maxDuration": 10,
      "excludeFiles": "{examples,.local}/**/*.json"
    }
  },
  "crons": []
}

