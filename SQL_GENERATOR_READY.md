# ✅ SQL Generator Ready!

Утилита для генерации готовых SQL команд создана и протестирована.

---

## 🚀 Быстрый старт

### По User ID (быстрее):

```bash
npm run sql:topup:user -- --user 1a13c194-06ce-4d18-8b36-b9eaf0a5d5d3 --amount 100
```

### По Telegram ID:

```bash
npm run sql:topup:tg -- --telegram 250474388 --amount 100
```

### Кастомные параметры:

```bash
npm run sql:topup -- --user <uuid> --amount 500
npm run sql:topup -- --telegram <id> --amount -50
```

---

## 📋 Что делает утилита

1. ✅ Генерирует **готовый SQL** без плейсхолдеров
2. ✅ Красивый вывод с рамками
3. ✅ Не выполняет команды (только генерирует)
4. ✅ Включает проверочные запросы

---

## 💡 Примеры вывода

### Режим User ID

```
╔══════════════════════════════════════════════════════════════════════╗
║ 📋 COPY THIS SQL TO SUPABASE SQL EDITOR                              ║
╠══════════════════════════════════════════════════════════════════════╣
-- Top-up 100 credits for user 1a13c194-06ce-4d18-8b36-b9eaf0a5d5d3
SELECT * FROM public.adjust_credits(
  p_user_id := '1a13c194-06ce-4d18-8b36-b9eaf0a5d5d3'::uuid,
  p_amount := 100,
  p_type := 'bonus',
  p_description := 'Manual top-up via script'
);
╚══════════════════════════════════════════════════════════════════════╝
```

### Режим Telegram ID

Генерирует 3 варианта:
1. **Step 1:** Найти `auth_user_id` по `telegram_id`
2. **Step 2:** Начислить кредиты (нужно подставить UUID из Step 1)
3. **DO $$:** Автоматически выполнить оба шага

---

## 📂 Созданные файлы

1. ✅ `scripts/genSqlTopup.ts` - Генератор SQL
2. ✅ `docs/SQL_GENERATOR.md` - Документация
3. ✅ `SQL_GENERATOR_READY.md` - Этот файл
4. ✅ `package.json` - Добавлены npm scripts

---

## 🎯 Сценарии использования

### Начислить бонус новому пользователю

```bash
npm run sql:topup -- --telegram 250474388 --amount 50
# Копируем и выполняем в Supabase SQL Editor
```

### Возврат средств

```bash
npm run sql:topup -- --user <uuid> --amount 10
```

### Списать кредиты

```bash
npm run sql:topup -- --user <uuid> --amount -50
```

---

## ✅ NPM Scripts

```json
{
  "sql:topup:tg": "tsx scripts/genSqlTopup.ts --telegram 250474388 --amount 100",
  "sql:topup:user": "tsx scripts/genSqlTopup.ts --user <uuid> --amount 100",
  "sql:topup": "tsx scripts/genSqlTopup.ts"
}
```

**Использование:**

```bash
# Предустановленные значения
npm run sql:topup:tg
npm run sql:topup:user

# Кастомные значения
npm run sql:topup -- --user <uuid> --amount 500
```

---

## 🔐 Безопасность

- ✅ Скрипт **НЕ подключается** к Supabase
- ✅ Скрипт **НЕ выполняет** SQL
- ✅ Скрипт только **генерирует текст**
- ✅ Вы сами решаете выполнять или нет

---

## 📖 Полная документация

См. `docs/SQL_GENERATOR.md` для:
- Детальных примеров
- Сценариев использования
- FAQ
- Идей для расширения

---

## 🎉 Готово к использованию!

**Попробуйте прямо сейчас:**

```bash
npm run sql:topup:user
```

Скопируйте вывод и вставьте в **Supabase SQL Editor** → **Run**

**Happy SQL generation! 🚀**

