# ✅ Русификация UI + SF Pro шрифт

Переведены все строки в модале "Split Grid Settings" и контекст-меню на русский язык. Добавлен Apple SF Pro шрифт во всё приложение.

---

## 📦 Что сделано

### A) Русификация интерфейса

#### 1. Split Grid Settings Modal
**Файл:** `src/components/SplitGridSettingsModal.tsx`

**Переводы:**
- ✅ "Split Grid Settings" → "Настройки разбиения сетки"
- ✅ "Number of Images" → "Количество изображений"
- ✅ "Grid will be split into 2×3 = 6 images" → "Сетка будет разбита на 2×3 = 6 изображений"
- ✅ "Default Prompt" → "Базовый промпт"
- ✅ "Enter prompt that will be applied to all generated images..." → "Введите промпт, который применится ко всем создаваемым изображениям…"
- ✅ "Each prompt node can be edited individually after creation" → "После создания каждый промпт-узел можно отредактировать отдельно."
- ✅ "Generate Node Settings" → "Параметры узла генерации"
- ✅ "Model" → "Модель"
- ✅ "Aspect Ratio" → "Соотношение сторон"
- ✅ "Resolution" → "Разрешение"
- ✅ "Google Search" → "Использовать Google Search"
- ✅ "Cancel" → "Отмена"
- ✅ "Create N Generate Sets" → "Создать N наборов генерации"

**Бонус: Русское склонение!**
```typescript
// 1 набор / 2–4 набора / 5+ наборов
const pluralizeSets = (count: number): string => {
  if (count % 10 === 1 && count % 100 !== 11) return "набор";
  if ([2,3,4].includes(count % 10) && ![12,13,14].includes(count % 100)) return "набора";
  return "наборов";
};

// 1 изображения / 2–4 изображений / 5+ изображений
const pluralizeImages = (count: number): string => { ... };
```

**Результат кнопки:**
- 1 → "Создать 1 набор генерации"
- 4 → "Создать 4 набора генерации"
- 6 → "Создать 6 наборов генерации"

---

#### 2. Connection Drop Menu (Контекст-меню)
**Файл:** `src/components/ConnectionDropMenu.tsx`

**Переводы:**
- ✅ "Add {type} node" → "Добавить узел ({type})"
  - "image" → "изображения"
  - "text" → "текста"
- ✅ "Generate Image" → "Генерация изображения"
- ✅ "LLM Generate" → "LLM-генерация"
- ✅ "Image Input" → "Ввод изображения"
- ✅ "Annotate" → "Аннотация"
- ✅ "Prompt" → "Промпт"
- ✅ "Split Grid Node" → "Узел разбиения сетки"
- ✅ "Split Grid Now" → "Разбить сетку сейчас"
- ✅ "Output" → "Вывод"
- ✅ "navigate" → "навигация"
- ✅ "select" → "выбрать"

**НЕ переведены (брендовые названия):**
- ✅ Nano Banana
- ✅ Nano Banana Pro
- ✅ Veo 3.1
- ✅ Seedream

---

### B) SF Pro шрифт (как на iPhone)

#### 1. globals.css
**Файл:** `src/app/globals.css`

Обновлен font-family stack:
```css
@theme {
  --font-family-sans: 
    -apple-system,
    BlinkMacSystemFont,
    "SF Pro Display",
    "SF Pro Text",
    system-ui,
    "Segoe UI",
    Roboto,
    "Helvetica Neue",
    Arial,
    sans-serif;
}

body {
  font-family: var(--font-family-sans);
}
```

#### 2. tailwind.config.ts (НОВЫЙ ФАЙЛ)
**Файл:** `tailwind.config.ts`

```typescript
export default {
  theme: {
    extend: {
      fontFamily: {
        sans: [
          "-apple-system",
          "BlinkMacSystemFont",
          "SF Pro Display",
          "SF Pro Text",
          "system-ui",
          "Segoe UI",
          "Roboto",
          "Helvetica Neue",
          "Arial",
          "sans-serif",
        ],
      },
    },
  },
};
```

**Применяется автоматически:**
- ✅ Все компоненты используют `font-sans` (default)
- ✅ Модалы
- ✅ Меню
- ✅ Ноды
- ✅ Кнопки
- ✅ Текстовые поля

---

## 🎯 Где проверить

### 1. Split Grid Settings Modal

**Шаги:**
1. Создать **Split Grid Node**
2. Дважды кликнуть на ноду
3. Откроется модал "Настройки разбиения сетки"
4. Увидите:
   - Заголовок: "Настройки разбиения сетки"
   - Лейбл: "Количество изображений"
   - Подсказка: "Сетка будет разбита на 2×3 = 6 изображений"
   - Placeholder: "Введите промпт, который применится ко всем создаваемым изображениям…"
   - Кнопка: "Создать 6 наборов генерации"

### 2. Connection Drop Menu

**Шаги для IMAGE меню:**
1. Создать **Image Input Node**
2. Начать тащить из output handle (зелёная точка справа)
3. Отпустить в пустом месте
4. Откроется меню "Добавить узел (изображения)"
5. Увидите опции:
   - "Аннотация"
   - "Генерация изображения"
   - "Узел разбиения сетки"
   - "Разбить сетку сейчас"
   - "Вывод"

**Шаги для TEXT меню:**
1. Создать **Prompt Node**
2. Начать тащить из output handle (синяя точка справа)
3. Отпустить в пустом месте
4. Откроется меню "Добавить узел (текста)"
5. Увидите опции:
   - "Генерация изображения"
   - "LLM-генерация"

### 3. SF Pro шрифт

**Как проверить:**
1. Открыть DevTools (F12)
2. Inspect любой текст
3. Computed styles → font-family
4. Должен быть: `-apple-system, BlinkMacSystemFont, "SF Pro Display"...`

**На macOS:**
- Используется SF Pro Display/Text (нативный шрифт Apple)
- Выглядит как iOS/iPadOS

**На Windows:**
- Используется Segoe UI
- Близкий fallback

**На Linux:**
- Используется system-ui или Roboto

---

## 📂 Измененные файлы

### Обновленные (2)
1. **`src/components/SplitGridSettingsModal.tsx`**
   - Все строки переведены
   - Добавлены функции склонения (pluralize)
   - font-medium для заголовков

2. **`src/components/ConnectionDropMenu.tsx`**
   - Все labels переведены
   - Keyboard shortcuts переведены
   - Динамический заголовок с типом

### Новые/обновленные (2)
3. **`src/app/globals.css`**
   - SF Pro font stack в @theme
   - body использует var(--font-family-sans)

4. **`tailwind.config.ts`** (НОВЫЙ)
   - Tailwind конфигурация с SF Pro

---

## ✅ Качество перевода

### Технический язык ✅
- "Настройки" вместо "Опции"
- "Параметры узла" вместо "Настройки узла"
- "Соотношение сторон" вместо "Аспект-ратио"

### Грамотность ✅
- "Нажмите для загрузки" (а не "Кликните")
- "Применится" (а не "будет применён")
- Правильные склонения

### Консистентность ✅
- "Узел" везде (не "нода")
- "Промпт" (не "запрос")
- "Генерация" (не "создание")

---

## 🎨 Визуальные улучшения

### Шрифт
- ✅ Более читаемый (SF Pro оптимизирован для экранов)
- ✅ Лучшая кернинг и tracking
- ✅ Профессиональный вид (как Apple приложения)

### Модал
- ✅ `font-medium` для заголовков (более выразительно)
- ✅ `text-sm` для labels (читаемость)
- ✅ `text-xs` для подсказок (иерархия)

### Меню
- ✅ `font-medium` для опций
- ✅ Выделение при hover/selected

---

## 📊 До и После

### До:
```
Split Grid Settings
Number of Images: 6
Create 6 Generate Sets

Add image node
  Generate Image
  LLM Generate
```

### После:
```
Настройки разбиения сетки
Количество изображений: 6
Создать 6 наборов генерации

Добавить узел (изображения)
  Генерация изображения
  LLM-генерация
```

---

## 🔤 Шрифт Stack Details

### Prioritization:
1. **-apple-system** - macOS/iOS native
2. **BlinkMacSystemFont** - Older macOS
3. **SF Pro Display/Text** - Explicit SF Pro
4. **system-ui** - Cross-platform system font
5. **Segoe UI** - Windows
6. **Roboto** - Android/Linux
7. **Helvetica Neue** - Fallback
8. **Arial** - Universal fallback
9. **sans-serif** - Browser default

### Rendering:
- **macOS 10.11+:** SF Pro Display/Text
- **Windows 10+:** Segoe UI
- **Android:** Roboto
- **Linux:** system-ui или Roboto

---

## 💡 Дополнительные детали

### Склонение работает так:
```typescript
pluralizeSets(1)  // "набор"
pluralizeSets(2)  // "набора"
pluralizeSets(5)  // "наборов"
pluralizeSets(11) // "наборов" (специальный случай)
pluralizeSets(21) // "набор"
```

### Font weights используются:
- `font-medium` - 500 (заголовки модалов)
- `font-normal` - 400 (основной текст)
- `text-sm` - 14px (labels)
- `text-xs` - 12px (hints)

---

## ✅ Checklist

- [x] ✅ Все строки модала переведены
- [x] ✅ Все строки меню переведены
- [x] ✅ Брендовые названия НЕ переведены
- [x] ✅ Русское склонение для чисел
- [x] ✅ SF Pro шрифт в globals.css
- [x] ✅ tailwind.config.ts создан
- [x] ✅ font-medium для заголовков
- [x] ✅ Технический язык использован
- [x] ✅ Грамматика проверена

---

**Happy localizing! 🇷🇺**

